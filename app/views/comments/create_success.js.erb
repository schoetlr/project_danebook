console.log("COMMENT CREATING W AJAX successful");


if( $(".newsfeed-post-form").length > 0 ){
  
  var comment = "<%= j(render partial: "newsfeed/comment", locals: {comment: @comment}) %>"

  var $comment = $(comment);
  
  var postId = "<%= @comment.commentable_id %>"
  

  $comment.appendTo($("#post-" + postId)).hide().slideDown();
  $("textarea#comment_content").val("");

  
  //$post.prependTo("#newsfeed-posts").hide().slideDown();
  //$("textarea#post_content").val("");
}




if( $("#post-feed").length > 0 ){
  var comment = "<%= j(render partial: "posts/comment", locals: {comment: @comment}) %>"

  var $comment = $(comment);

  var postId = "<%= @comment.commentable_id %>"
  

  $comment.appendTo($("#post-" + postId)).hide().slideDown();
  $("textarea#comment_content").val("");


}

if( $("#photo-comments").length > 0){
  
  var comment = "<%= j(render partial: "photos/comment", locals: {comment: @comment}) %>"

  var $comment = $(comment);
  

  $comment.appendTo($("#photo-comments")).hide().slideDown();
  $("textarea#comment_content").val("");


}